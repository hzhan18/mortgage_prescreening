[project]
name = "mortgage-prescreen"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "Flask==3.0.3",
    "Flask-SQLAlchemy==3.1.1",
    "anthropic==0.40.0",
    "python-dotenv==1.0.1",
    "psycopg2-binary==2.9.9",
]

[tool.vercel]
entrypoint = "app:app"
